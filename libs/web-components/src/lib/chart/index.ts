// Only export types - safe for all frameworks
export type { ChartData } from './chart-types';

// Don't export the actual components from the main barrel
// These will be available through a separate entry point
